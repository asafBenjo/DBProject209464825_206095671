-- הצגת כל הנתונים מטבלת המשתמשים
SELECT * FROM "Users";

-- הצגת כל הנתונים מטבלת סוכני התמיכה
SELECT * FROM Support_Agent;

-- הצגת כל הנתונים מטבלת סוגי הבעיות
SELECT * FROM Issue_Types;

-- הצגת כל הנתונים מטבלת קריאות השירות
SELECT * FROM Support_Tickets;

-- הצגת כל הנתונים מטבלת תגובות לתמיכה
SELECT * FROM Support_Responses;

-- הצגת כל הנתונים מטבלת סטטוסים של קריאות שירות
SELECT * FROM Ticket_Status;
